---
title: Predicción diaria sobre el partido ganador de las elecciones locales del 26M en Madrid.
---

¿Cómo de probable es que estos partidos sean el primero en número de concejales?



_________________

```{r graph0, echo=FALSE, out.width = '20%'}

highchart() %>% 
  hc_xAxis(categories = first_val_set$datetime2, tickInterval = 1) %>% 
  hc_add_series(name = "PSOE", data = first_val_set$PSOE , color = "#FF3827") %>% 
  hc_add_series(name = "PP", data = first_val_set$PP, color= "#04AAFF") %>% 
  hc_add_series(name = "Ciutadans", data = first_val_set$Ciutadans, color = "#FF9300") %>% 
  hc_add_series(name = "Compromís", data = first_val_set$`Compromís`, color= "#e65100") %>% 
  hc_add_series(name = "Otro/a", data = first_val_set$Other, color= "#bdbdbd") %>% 
  hc_tooltip(crosshairs = TRUE, backgroundColor = "#FBFEFF",
             shared = TRUE, borderWidth = 0, sort = TRUE,  table = TRUE)



```

_____

Contacto: Alberto López Ortega _alberto.lopezortega@uzh.ch_ , [bertous](https://twitter.com/bertous)

